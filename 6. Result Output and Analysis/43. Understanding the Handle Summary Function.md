**Code:**

```javascript
import http from "k6/http";

export default function() {
	http.get("https://test.k6.io");
}

export function handleSummary(data) {
	return {
		"summary.json": JSON.stringify(data)
	};
}
```

**Explanation:**
The **handleSummary()** is the built in function which is used to store the output data in the of a file. The **data** object will be passed automatically by **K6**.

