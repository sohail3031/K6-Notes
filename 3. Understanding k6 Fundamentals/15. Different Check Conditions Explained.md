The **check()** is used to check if the request is meeting certain condition at each iteration.

**Code:**

```javascript
import http from "k6/http";
import { check } from "k6";

export default function() {
	let res = http.get("https://test-api.k6.io/public/crocodiles/1/");
	
	check(res, {
		"status is 200": (r) => r.status === 200
	});
}
```

**Code:**

```javascript
import http from "k6/http";
import { check } from "k6";

export default function() {
	let res = http.get("https://test-api.k6.io/public/crocodiles/1/");
	
	check(res, {
		"status is 200": (r) => r.status === 200,
		"response time is less than 500ms": (r) => r.timings.duration < 500
	});
	
	console.log(`Complete Response Object: ${JSON.stringify(res, null, 2)}`);
}
```

**Code:**

```javascript
import http from "k6/http";
import { check } from "k6";

export default function() {
	let url = "https://httpbin.org/post"
	let res = http.post(url, "Hello World!");
	let result = check(res, {
		"Application says hello": (r) => r.body.includes("Bonjour!")
	});
	
	if (resuslt) {
		console.log(`Check success. Resposne body was: ${res.body}`);
	} else {
		console.log(`Check failed. Response body was: ${res.body}`);
	}
}
```

**Code:**

```javascript
import http from "k6/http";
import { check } from "k6";

export default function() {
	let url = "https://httpbin.org/post"
	let res = http.post(url, "Hello World!");
	let result = check(res, {
		"Application says hello": (r) => r.body.includes("Hello World!")
	});
	
	if (resuslt) {
		console.log(`Check success. Resposne body was: ${res.body}`);
	} else {
		console.log(`Check failed. Response body was: ${res.body}`);
	}
}
```

