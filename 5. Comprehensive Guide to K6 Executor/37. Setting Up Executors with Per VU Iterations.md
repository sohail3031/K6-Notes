**Code:**

```javascript
import http from "k6/http";
import { sleep } from "k6";

export let options = {
	scenarios: {
		per_vu_iterations_scenario: {
			executor: "per-vu-iterations", // iterations performed by all VUs
			vus: 10, // numebr of VUs
			iterations: 5, // number of iterations
			maxDuration: "1m" // maximum duration to run the test
		}
	}
};

export default function() {
	http.get("https://test-api.k6.io/public/crocodiles/");
	
	sleep(1);
}
```

**Explanation:**

![[37.png]]

