**Code:**

```javascript
import http from "k6/http";
import { sleep } from "k6";

export let options = {
	scenarios: {
		contacts: {
			executor: "ramping-arrival-rate", // maintain constant requests doring ramp-up & ramp-down
			startRate: 300, // rate at the start
			timeUnit: "20s", // start rate iterations per second
			preAllocatedVUs: 20, // pre-allocated vus
			stages: [
				{ duration: "20s", target: 300 }, // ramping-up stage
				{ duration: "20s", target: 600 }, // ramping-up stage
				{ duration: "20s", target: 600 }, // staging
				{ duration: "20s", target: 60 } // ramp-down
			]
		}
	}
};

export default function() {
	http.get("https://test.k6.io/contacts.php");
	
	sleep(1);
}
```

**Explanation:**

![[41.png]]
