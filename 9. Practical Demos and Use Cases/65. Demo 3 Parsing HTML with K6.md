Time comes when you need to verify if certain elements are present on a web page. K6 allows to parse HTML. See the below sample codes:

**Code 1:**

```javascript
import { parseHTML } from "k6/html";
import http from "k6/http";

export default function() {
	const res = http.get("https://k6.io");
	const doc = parseHTML(res.body); // equivalent to res.html()
	const pageTitle = doc.find("head title").text();
	const langAttr = doc.find("html").attr("lang");
	
	console.log("pageTitle", pageTitle);
	console.log("langAttr", langAttr);
}
```

**Code 2:**

```javascript
import { parseHTML } from "k6/html";
import { sleep } from "k6";

export default function() {
	const content = `
	<dl>
		<dt id="item-1">Value Item 1</dt>
		<dt id="item-2">Value Item 2</dt>
	</dl>
	`;
	const sel = parseHTML(content).find("dl").children();
	const el1 = sel.get(0);
	const el2 = sel.get(1);
	
	console.log(el1.nodeName());
	console.log(el1.id());
	console.log(el1.textContent());
	
	console.log(el2.nodeName());
	console.log(el2.id());
	console.log(el2.textContent());
	
	sleep(1);
}
```
